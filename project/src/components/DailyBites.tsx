import { useState } from 'react';
import { Check, ChevronRight, Play, X, Zap } from 'lucide-react';

const bites = [
  { title: "Snell's Law in 30s", subject: 'Physics · Optics', question: 'When light moves from air into glass, what happens to its speed?', answer: 'It slows down because glass is optically denser than air.' },
  { title: 'The DNA Double Helix', subject: 'Biology · Genetics', question: 'Which bases pair together in DNA?', answer: 'Adenine pairs with Thymine, and Cytosine pairs with Guanine.' },
  { title: 'Why Does pH Matter?', subject: 'Chemistry · Acids', question: 'A solution with pH 3 is how many times more acidic than pH 5?', answer: '100 times more acidic, because pH is a logarithmic scale.' },
  { title: 'Newton’s Third Law', subject: 'Physics · Mechanics', question: 'What is the reaction force when you push a wall?', answer: 'The wall pushes back on you with an equal and opposite force.' },
];

export default function DailyBites() {
  const [activeQuiz, setActiveQuiz] = useState<number | null>(null);
  const [answered, setAnswered] = useState(false);
  const close = () => { setActiveQuiz(null); setAnswered(false); };
  return (
    <section id="daily-bites" className="bg-[#0d1b32] px-5 py-24 lg:px-8 lg:py-32"><div className="mx-auto max-w-[1240px]"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><div className="section-kicker text-coral">Daily / Micro-lessons</div><h2 className="mt-4 text-4xl font-black tracking-[-.05em] sm:text-6xl">One idea.<br /><span className="text-coral">Thirty seconds.</span></h2></div><p className="max-w-[360px] text-sm leading-6 text-slate-400">Fast, focused explanations for the moments between classes. Scroll, play, solve.</p></div><div className="mt-14 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{bites.map((bite, index) => <article key={bite.title} className="group relative min-h-[270px] overflow-hidden rounded-[1.5rem] border border-white/10 bg-ink p-6 transition hover:-translate-y-2 hover:border-coral/40"><div className="absolute -right-8 -top-8 h-28 w-28 rounded-full border border-coral/20 bg-coral/10 transition group-hover:scale-150" /><div className="relative flex items-center justify-between"><span className="rounded-full bg-coral/10 px-2.5 py-1 text-[9px] font-black uppercase tracking-wider text-coral">{bite.subject}</span><span className="text-[10px] font-black text-slate-600">0{index + 1}</span></div><h3 className="relative mt-12 text-xl font-black leading-tight">{bite.title}</h3><button className="group/play mt-8 flex items-center gap-2 text-xs font-black uppercase tracking-wider text-white transition hover:scale-105"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-coral text-white transition group-hover/play:bg-white group-hover/play:text-coral"><Play size={14} fill="currentColor" /></span> Watch bite</button><button onClick={() => setActiveQuiz(index)} className="mt-5 flex items-center gap-2 text-[10px] font-black uppercase tracking-wider text-slate-400 transition hover:scale-105 hover:text-coral">Test my knowledge <ChevronRight size={14} /></button></article>)}</div></div>{activeQuiz !== null && <div className="modal-backdrop fixed inset-0 z-[70] flex items-center justify-center bg-black/70 px-5 backdrop-blur-sm" onClick={close}><div className="modal-panel relative w-full max-w-md rounded-2xl bg-white p-7 text-ink" onClick={(e) => e.stopPropagation()}><button onClick={close} className="absolute right-4 top-4 text-slate-400"><X size={20} /></button><div className="flex items-center gap-2 text-[10px] font-black uppercase tracking-wider text-coral"><Zap size={13} /> Quick check</div><h3 className="mt-4 text-2xl font-black">{bites[activeQuiz].title}</h3><p className="mt-5 text-base font-bold leading-6">{bites[activeQuiz].question}</p>{answered ? <div className="mt-6 rounded-xl bg-emerald-50 p-4 text-sm leading-6 text-emerald-800"><div className="flex items-center gap-2 font-black"><Check size={16} /> Concept unlocked</div><p className="mt-2">{bites[activeQuiz].answer}</p></div> : <button onClick={() => setAnswered(true)} className="mt-6 w-full rounded-xl bg-coral px-5 py-3.5 text-sm font-black text-white transition hover:scale-105">Reveal answer</button>}</div></div>}</section>
  );
}
