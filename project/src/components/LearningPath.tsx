import { useState } from 'react';
import { BrainCircuit, Check, Circle, Lock, Sparkles } from 'lucide-react';

const nodes = [
  { title: 'Physics', subtitle: 'Mechanics', done: true },
  { title: 'Optics', subtitle: 'Ray diagrams', weak: true },
  { title: 'Electrostatics', subtitle: 'Fields & charge', done: false },
  { title: 'Current Electricity', subtitle: 'Circuits', locked: true },
];

export default function LearningPath() {
  const [loading, setLoading] = useState(false);
  const generate = () => { setLoading(true); window.setTimeout(() => setLoading(false), 1800); };
  return <section id="learning-path" className="bg-[#0d1b32] px-5 py-24 lg:px-8 lg:py-32"><div className="mx-auto max-w-[1240px]"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><div className="section-kicker text-coral">Adaptive / AI learning path</div><h2 className="mt-4 text-4xl font-black tracking-[-.05em] sm:text-6xl">Your syllabus,<br /><span className="text-coral">but smarter.</span></h2></div><button onClick={generate} disabled={loading} className="flex w-fit items-center gap-3 rounded-full bg-coral px-6 py-3.5 text-sm font-black text-white transition hover:scale-105 disabled:cursor-wait disabled:opacity-80">{loading ? <><span className="h-4 w-4 animate-spin rounded-full border-2 border-white/30 border-t-white" /> Building your plan</> : <><Sparkles size={16} /> Generate AI Study Plan</>}</button></div><div className="mt-14 rounded-[2rem] border border-white/10 bg-ink p-6 sm:p-10"><div className="mb-8 flex items-center gap-3"><div className="rounded-xl bg-coral/10 p-3 text-coral"><BrainCircuit size={22} /></div><div><div className="text-sm font-black">Physics skill tree</div><div className="text-xs text-slate-500">Class 12 · Adaptive to your practice history</div></div></div><div className="grid gap-3 md:grid-cols-4 md:items-center">{nodes.map((node, i) => <div key={node.title} className="relative flex items-center md:block"><div className={`relative z-10 flex h-16 w-16 shrink-0 items-center justify-center rounded-full border-2 ${node.done ? 'border-emerald-400 bg-emerald-400/10 text-emerald-400 shadow-[0_0_26px_rgba(52,211,153,.25)]' : node.weak ? 'border-coral bg-coral/10 text-coral shadow-[0_0_28px_rgba(255,107,0,.35)]' : node.locked ? 'border-white/10 bg-white/[.03] text-slate-600' : 'border-sky-400/60 bg-sky-400/10 text-sky-400'}`}>{node.done ? <Check size={22} /> : node.locked ? <Lock size={19} /> : <Circle size={19} />}</div><div className="ml-4 md:ml-0 md:mt-4"><div className="text-sm font-black">{node.title}</div><div className="mt-1 text-[11px] text-slate-500">{node.subtitle}</div><div className={`mt-2 text-[9px] font-black uppercase tracking-wider ${node.done ? 'text-emerald-400' : node.weak ? 'text-coral' : 'text-slate-600'}`}>{node.done ? 'Mastered' : node.weak ? 'Needs focus' : node.locked ? 'Locked' : 'In progress'}</div></div>{i < nodes.length - 1 && <div className="mx-5 h-8 w-px bg-gradient-to-b from-coral/50 to-white/10 md:absolute md:left-16 md:right-[-3rem] md:top-8 md:mx-0 md:h-px md:w-auto" />}</div>)}</div><div className="mt-10 flex flex-wrap items-center gap-5 border-t border-white/10 pt-5 text-[10px] font-black uppercase tracking-wider text-slate-500"><span className="flex items-center gap-2"><span className="h-2 w-2 rounded-full bg-emerald-400" /> Mastered</span><span className="flex items-center gap-2"><span className="h-2 w-2 rounded-full bg-coral" /> Needs focus</span><span className="flex items-center gap-2"><span className="h-2 w-2 rounded-full bg-sky-400" /> In progress</span></div></div></div></section>;
}
